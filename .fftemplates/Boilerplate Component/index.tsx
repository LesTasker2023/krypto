import './styles.scss';

import React from 'react';

import type Props from './types';

export const <FTName> = ({ data }: Props) => {
   

  return <div className='<FTName | kebabcase>'>{data}</div>
}
